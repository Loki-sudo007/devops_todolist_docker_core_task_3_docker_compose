# Instructions

To build and start the containers, run:

docker compose up --build

To launch the application, go to:

http://localhost:8081/

To stop the containers, run:

docker compose down

The application uses MySQL database.
Todos are stored in MySQL with a persistent Docker volume.